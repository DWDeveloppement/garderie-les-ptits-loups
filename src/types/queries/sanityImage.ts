/* Les parallaxes sont les parallaxes de la garderie doiven être typées ici. */

import type { SanityImage } from '@/sanity/types/core/image'

export type ParalaxImageProps = {
	id: string
	image: SanityImage
}
